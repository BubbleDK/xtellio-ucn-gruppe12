export const use = {
  // Configure viewport and download options.
  viewport: { width: 1280, height: 720 },
  ignoreHTTPSErrors: true,

  // Browser options
  headless: true,

  // Context options
  launchOptions: {
    slowMo: 50,
  },
};